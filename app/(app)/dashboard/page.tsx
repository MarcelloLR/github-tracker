export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p>
        Global stats, contribution calendar, goals, streaks, and your top repos &amp;
        orgs will render here.
      </p>
      <p>
        <em>Scaffold — data wiring (discover → sync → compute-stats) in progress.</em>
      </p>
    </main>
  );
}
