export default function SettingsPage() {
  return (
    <main>
      <h1>Settings</h1>
      <p>
        Goals, sync interval, deep-dive budget, and public-portfolio toggle. (Scaffold —
        wired to server actions in app/actions.)
      </p>
    </main>
  );
}
